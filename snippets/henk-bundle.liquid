<style>
  .hb-wrapper {
    background: var(--scheme-6_solid, #f4f4f4);
    border: 2px solid var(--scheme-6_solid, #f4f4f4);
    border-radius: 10px;
    box-shadow: var(--vct-shadow-diffuse, 0 24px 48px -16px rgba(3,10,34,.09));
    padding: 15px;
    margin: 16px 0;
    font-family: var(--main_ff, inherit);
  }

  .hb-inner { display: flex; gap: 12px; align-items: flex-start; }
  .hb-main-product { flex: 1; min-width: 0; display: flex; flex-direction: column; align-items: center; position: relative; }

  .hb-main-img-wrap {
    width: 100%;
    aspect-ratio: 1;
    background: var(--scheme-1_bg, #fff);
    border-radius: 10px;
    border: 1px solid var(--scheme-1_bd, #ecedec);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hb-main-img-wrap img { width: 100%; height: 100%; object-fit: contain; padding: 8px; box-sizing: border-box; }
  .hb-main-info { display: flex; flex-direction: column; gap: 2px; margin-top: 8px; text-align: center; width: 100%; }
  .hb-main-vendor { font-size: 11px; color: var(--gray_text, #959595); font-weight: 500; }
  .hb-main-title { font-size: 11px; font-weight: 600; color: var(--primary_text, #030a22); line-height: 1.3; display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 2; overflow: hidden; }
  .hb-main-price { font-size: 13px; font-weight: 800; color: var(--price_color, #030a22); margin-top: 2px; }

  .hb-plus {
    position: absolute;
    right: -18px;
    top: 38%;
    width: 24px;
    height: 24px;
    background: var(--scheme-1_bg, #fff);
    border: 1px solid var(--scheme-1_bd, #ecedec);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    font-weight: 700;
    color: var(--gray_text, #959595);
    z-index: 1;
    transform: translateY(-50%);
  }

  .hb-upsell-grid { display: flex; gap: 12px; flex: 3; min-width: 0; }

  .hb-card {
    flex: 1;
    background: var(--scheme-1_bg, #fff);
    border: 1px solid var(--scheme-1_bd, #ecedec);
    border-radius: 10px;
    padding: 10px;
    display: flex;
    flex-direction: column;
    gap: 5px;
    cursor: pointer;
    user-select: none;
    transition: border-color 0.2s, background 0.2s, box-shadow 0.2s;
    min-width: 0;
    position: relative;
  }

  .hb-card:hover { border-color: var(--accent, #ff7e00); }
  .hb-card.is-checked { border-color: var(--accent, #ff7e00); background: #fffaf5; }
  .hb-card:focus, .hb-card *:focus { outline: none; box-shadow: none; }
  .hb-card-top { display: flex; align-items: flex-start; justify-content: space-between; gap: 4px; }

  .hb-custom-checkbox { display: block; cursor: pointer; flex-shrink: 0; }
  .hb-custom-checkbox input { display: none; }

  .hb-checkmark {
    width: 20px;
    height: 20px;
    border-radius: 5px;
    border: 2px solid var(--alto, #dbdbdb);
    background: var(--scheme-1_bg, #fff);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.15s ease;
  }

  .hb-checkmark svg { color: #fff; opacity: 0; transition: opacity 0.15s ease; }
  .hb-custom-checkbox input:checked + .hb-checkmark { background: var(--accent, #ff7e00); border-color: var(--accent, #ff7e00); }
  .hb-custom-checkbox input:checked + .hb-checkmark svg { opacity: 1; }

  .hb-discount-badge {
    background: var(--sale_label_bg, #e91607);
    color: var(--sale_label_text, #fff);
    font-size: 10px;
    font-weight: 700;
    padding: 2px 5px;
    border-radius: 5px;
    white-space: nowrap;
  }

  .hb-discount-badge--abs { display: none; }
  .hb-card-img { width: 100%; aspect-ratio: 1; object-fit: contain; border-radius: 6px; display: block; padding: 4px; box-sizing: border-box; }
  .hb-card-vendor { font-size: 10px; color: var(--gray_text, #959595); font-weight: 500; }
  .hb-card-title { font-size: 11px; font-weight: 600; color: var(--primary_text, #030a22); line-height: 1.3; display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 2; overflow: hidden; }
  .hb-card-prices { display: flex; align-items: center; gap: 4px; flex-wrap: wrap; }
  .hb-price-current { font-size: 12px; font-weight: 800; color: var(--price_color, #030a22); }
  .hb-price-old { font-size: 10px; color: var(--price_color_old, #e91607); text-decoration: line-through; }
  .hb-card-body-wrap { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 3px; }

  .hb-alt-btn {
    display: inline-flex;
    align-items: center;
    gap: 3px;
    font-size: 10px;
    color: var(--accent, #ff7e00);
    font-weight: 500;
    cursor: pointer;
    text-decoration: underline;
    text-underline-offset: 2px;
    margin-top: auto;
    line-height: 1.4;
  }

  .hb-alt-btn:hover { color: var(--primary_bg_btn_dark, #db6c00); }

  .hb-info-btn {
    position: absolute;
    bottom: 8px;
    right: 8px;
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: #fff3e8;
    border: none;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--accent, #ff7e00);
    font-size: 11px;
    font-weight: 700;
    font-family: Georgia, serif;
    cursor: pointer;
    text-decoration: none;
    line-height: 1;
    transition: background 0.15s, color 0.15s;
    z-index: 2;
  }

  .hb-info-btn:hover { background: var(--accent, #ff7e00); border: none; color: #fff !important; }

  .hb-alt-panel {
    margin-top: 12px;
    border: 1px solid var(--scheme-1_bd, #ecedec);
    border-radius: 10px;
    overflow: hidden;
    background: var(--scheme-1_bg, #fff);
  }

  .hb-alt-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 14px;
    border-bottom: 1px solid var(--scheme-1_bd, #ecedec);
  }

  .hb-alt-label { font-size: 12px; font-weight: 700; color: var(--primary_text, #030a22); }
  .hb-alt-close { font-size: 13px; color: var(--gray_text, #959595); cursor: pointer; line-height: 1; padding: 2px 4px; }
  .hb-alt-close:hover { color: var(--primary_text, #030a22); }
  .hb-alt-scroll-wrap { display: flex; align-items: center; }
  .hb-alt-scroll { display: flex; gap: 8px; padding: 12px; overflow-x: auto; -webkit-overflow-scrolling: touch; scrollbar-width: none; flex: 1; scroll-behavior: smooth; }
  .hb-alt-scroll::-webkit-scrollbar { display: none; }
  .hb-alt-arrow { display: none !important; }

  @media (min-width: 641px) {
    .hb-wrapper .hb-alt-arrow {
      display: flex !important;
      align-items: center !important;
      justify-content: center !important;
      flex-shrink: 0 !important;
      width: 26px !important;
      height: 26px !important;
      min-width: 0 !important;
      min-height: 0 !important;
      max-width: 26px !important;
      max-height: 26px !important;
      background: var(--scheme-1_bg_secondary, #efefef) !important;
      border: 1px solid var(--scheme-1_bd, #ecedec) !important;
      border-radius: 50% !important;
      cursor: pointer !important;
      color: var(--gray_text, #959595) !important;
      transition: border-color 0.2s, color 0.2s !important;
      margin: 0 4px !important;
      padding: 0 !important;
      box-shadow: none !important;
      font-size: 0 !important;
      line-height: 0 !important;
    }

    .hb-wrapper .hb-alt-arrow:hover {
      border-color: var(--alto, #dbdbdb) !important;
      color: var(--primary_text, #030a22) !important;
      background: var(--scheme-6_solid, #f4f4f4) !important;
    }

    .hb-wrapper .hb-alt-arrow:disabled { opacity: 0.3 !important; cursor: default !important; pointer-events: none !important; }
    .hb-wrapper .hb-alt-arrow svg { width: 12px !important; height: 12px !important; flex-shrink: 0 !important; }
  }

  .hb-alt-card {
    flex-shrink: 0;
    width: 110px;
    border: 1px solid var(--scheme-1_bd, #ecedec);
    border-radius: 10px;
    padding: 8px;
    cursor: pointer;
    position: relative;
    transition: border-color 0.2s;
  }

  .hb-alt-card:hover { border-color: var(--accent, #ff7e00); }
  .hb-alt-card--active { border-color: var(--accent, #ff7e00); background: #fffaf5; pointer-events: none; }
  .hb-alt-card img { width: 100%; aspect-ratio: 1; object-fit: contain; border-radius: 6px; display: block; }

  .hb-alt-discount-badge {
    position: absolute;
    top: 6px;
    right: 6px;
    background: var(--sale_label_bg, #e91607);
    color: var(--sale_label_text, #fff);
    font-size: 9px;
    font-weight: 700;
    padding: 2px 4px;
    border-radius: 4px;
  }

  .hb-alt-vendor { display: block; font-size: 9px; color: var(--gray_text, #959595); margin-top: 5px; }
  .hb-alt-title { display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 2; overflow: hidden; font-size: 10px; font-weight: 600; color: var(--primary_text, #030a22); line-height: 1.3; margin: 2px 0 4px; }
  .hb-alt-prices { display: flex; align-items: center; gap: 4px; flex-wrap: wrap; }

  .hb-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid var(--scheme-1_bd, #ecedec);
    flex-wrap: wrap;
  }

  .hb-footer-left { display: flex; flex-direction: column; gap: 3px; }
  .hb-total-wrap { display: flex; align-items: baseline; gap: 8px; }
  .hb-total-new { font-size: 22px; font-weight: 800; color: var(--price_color, #030a22); }
  .hb-total-old { font-size: 15px; color: var(--price_color_old, #e91607); text-decoration: line-through; }
  .hb-stock { font-size: 12px; color: var(--alert_valid, #ff7e00); font-weight: 500; }

  .hb-btn {
    background: var(--primary_bg_btn, #ff7e00) !important;
    color: var(--primary_btn_text, #fff) !important;
    border: none !important;
    border-radius: var(--btn_br, 28px) !important;
    padding: 12px 24px !important;
    font-size: 13px !important;
    font-weight: 700 !important;
    cursor: pointer !important;
    display: inline-flex !important;
    align-items: center !important;
    gap: 8px !important;
    transition: background 0.2s !important;
    white-space: nowrap !important;
    width: auto !important;
    min-width: 0 !important;
    height: auto !important;
    box-shadow: none !important;
    line-height: normal !important;
  }

  .hb-btn:hover { background: var(--primary_bg_btn_dark, #db6c00) !important; }
  .hb-btn:disabled { background: #ccc !important; cursor: default !important; }

  @media (max-width: 640px) {
    .hb-wrapper { padding: 12px; overflow: hidden; }
    .hb-inner { flex-direction: column; gap: 8px; }
    .hb-main-product { flex-direction: row; width: 100%; flex: none; gap: 12px; align-items: center; }
    .hb-main-img-wrap { width: 80px; height: 80px; flex-shrink: 0; aspect-ratio: unset; }
    .hb-main-info { text-align: left; margin-top: 0; flex: 1; }
    .hb-plus { position: static; transform: none; margin: 0; flex-shrink: 0; }
    .hb-upsell-grid { flex-direction: column; width: 100%; gap: 8px; flex: none; }
    .hb-card { flex-direction: row; align-items: center; gap: 8px; position: relative; padding: 10px; box-sizing: border-box; }
    .hb-card-top { flex-direction: column; align-items: center; flex-shrink: 0; gap: 0; }
    .hb-card-top .hb-discount-badge { display: none; }
    .hb-card-img { width: 64px; height: 64px; flex-shrink: 0; aspect-ratio: unset; }
    .hb-card-title { padding-right: 40px; }
    .hb-discount-badge--abs { display: inline-block; position: absolute; top: 10px; right: 10px; }
    .hb-info-btn { bottom: 8px; right: 8px; top: auto; }
    .hb-footer { flex-direction: column; align-items: flex-start; }
    .hb-btn { width: 100% !important; justify-content: center !important; }
  }
</style>
